const fast = Duration(milliseconds: 200);
const faster = Duration(milliseconds: 150);
const midSpeed = Duration(milliseconds: 350);
const midSlow = Duration(milliseconds: 450);
const slow = Duration(milliseconds: 600);
